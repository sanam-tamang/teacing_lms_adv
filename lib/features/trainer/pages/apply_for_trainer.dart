import 'package:flutter/material.dart';

class ApplyForTrainerPage extends StatefulWidget {
  const ApplyForTrainerPage({super.key});

  @override
  State<ApplyForTrainerPage> createState() => _ApplyForTrainerPageState();
}

class _ApplyForTrainerPageState extends State<ApplyForTrainerPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          // CustomText
        ],
      ),
    );
  }
}
